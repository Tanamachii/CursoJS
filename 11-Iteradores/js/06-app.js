let arreglo = ['Pedro','Eduardo','Tanamachi'];

arreglo.forEach((nombre,index) => {
    console.log(index, nombre);
})
  
//MAP CREAR UN ARREGLO NUEVO
const nuevoarreglo = arreglo.map(nombre => console.log( nombre));
  
